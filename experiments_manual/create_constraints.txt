CREATE CONSTRAINT ON (r:REGION) ASSERT r.r_regionkey IS NODE KEY

CREATE CONSTRAINT ON (n:NATION) ASSERT n.n_nationkey IS NODE KEY

CREATE CONSTRAINT ON (p:PART) ASSERT p.p_partkey IS NODE KEY

CREATE CONSTRAINT ON (s:SUPPLIER) ASSERT s.s_suppkey IS NODE KEY

CREATE CONSTRAINT ON (ps:PARTSUPP) ASSERT (ps.ps_partkey, ps.ps_suppkey) IS NODE KEY

CREATE CONSTRAINT ON (c:CUSTOMER) ASSERT c.c_custkey IS NODE KEY

CREATE CONSTRAINT ON (o:ORDERS) ASSERT o.o_orderkey IS NODE KEY

CREATE CONSTRAINT ON (l:LINEITEM) ASSERT (l.l_orderkey,l.l_linenumber) IS NODE KEY
